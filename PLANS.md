# PLANS.md

## 0. 文档状态
- 更新时间：2026-05-06
- 责任角色：orchestrator
- 阶段：工程 MVP 验证
- 关联任务：TASK-000

---

## 1. 项目目标
LightDesign 当前目标是验证一个可持续迭代的网站产品与生成流程：从用户输入、需求澄清、设计约束、页面生成到验收交付，形成可复用的多 agent 协作闭环。

---

## 2. 探索原则
1. 先验证流程，再扩大功能。
2. 先固化输入、输出和验收标准，再进入工程实现。
3. 每次迭代必须能留下可复用文档、模板或决策。
4. 所有任务由 orchestrator 统一拆分、追踪和回收。

---

## 3. 当前版本假设

| 编号 | 假设 | 验证方式 | 状态 |
| --- | --- | --- | --- |
| H-001 | 用户需要用自然语言快速生成网站产品方案 | 访谈 / 原型演示 | todo |
| H-002 | 多 agent 分工能提升需求、UI、测试和实现质量 | 小任务试运行 | todo |
| H-003 | 标准状态文件能降低任务漂移和上下文丢失 | 连续 3 个任务复盘 | todo |

---

## 4. 近期路线

| 阶段 | 目标 | 主要产出 | 完成标准 |
| --- | --- | --- | --- |
| P0 | 建立最小协作骨架 | 状态文件、任务模板、决策日志 | ✅ 完成 |
| P1 | 明确网站产品核心流程 | PRD、场景收敛、首版边界 | ✅ 完成 |
| P2 | 交互低保真设计 | 核心页面线框、交互状态说明、高保真原型 | ✅ 完成 |
| P3 | 工程 MVP 垂直切片 | Next.js 骨架、单次生图闭环、预览导出 | ✅ 完成 |
| P4 | 验证与复盘 | 本地运行 MVP，跑验收清单，决定下一步 | ✅ 完成：TASK-009 已验收 |

---

## 5. 生成流程草案
1. 用户输入目标、行业、风格、内容素材和约束。
2. product 澄清用户场景、范围和成功标准。
3. ui 定义信息架构、关键页面、组件和状态。
4. engineer-* 生成页面、样式、数据结构或脚本。
5. test 按验收清单检查内容完整性、交互、响应式和可访问性。
6. orchestrator 汇总结果，更新任务、决策和 PR 说明。

---

## 6. 风险清单

| 风险 | 影响 | 处理方式 | 状态 |
| --- | --- | --- | --- |
| 目标过早扩大 | 任务难以闭环 | 每次只推进一个可验收产出 | open |
| UI 风格不稳定 | 生成结果不可复用 | 在 UI_SPEC.md 中沉淀规范 | open |
| 多 agent 输出冲突 | 文档和实现不一致 | 由 orchestrator 做 checkpoint 裁决 | open |
| 文档与实现漂移 | 验收口径失真 | 维护文档基线，明确“已实现 / 待补齐 / 已变更” | open |
| 上传图片未进入生成链路 | MVP 核心价值不足 | 已修复前端提交与后端读取，并验证 mock / APIMART 请求体链路 | resolved |
| lint 未通过 | CI/协作门禁阻塞 | 已修复到 0 error，剩余 4 个 `<img>` warning | resolved |
| 真实生成质量样本不足 | 可能影响商用验收 | TASK-016 已完成 1 张真实商品图单样本；TASK-019 继续规划 3-5 张扩样本 | open |
| 大图上传 payload 风险 | 原图 data URL 过大可能增加外部调用失败率和耗时 | TASK-020 已实现上传前长边 1024 降采样，待验收 | mitigated |
