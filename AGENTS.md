# AGENTS.md

## 1. 文档目的与适用范围
本文件用于定义本项目的多 Agent 协作机制、任务流转规则与交付规范。

适用范围：仓库根目录及其全部子目录。

项目阶段声明：当前处于产品与流程探索阶段，以需求澄清、体验验证、流程固化为优先，不以大规模代码开发为目标。

---

## 2. 协作总原则
1. 单一总控原则：由 orchestrator 统一进行任务分解、分派、检查点回收与状态裁决。
2. 单主任务单主线程原则：一个主任务只对应一个主线程，禁止同一主任务并行建立多个主线程。
3. 先判断后执行原则：任务留在主线程、拆分子线程、或进入 worktree，必须先由 orchestrator 给出判断与理由，再由用户确认后执行。
4. 文档先行原则：执行前先明确目标、边界、完成标准与交付物。
5. PR 唯一出口原则：所有可交付成果必须通过 PR 交付。
6. 安全发布原则：默认允许自动创建 PR；禁止自动合并 main，main 合并必须由用户明确批准。

---

## 3. 角色与职责边界

### 3.1 orchestrator（总控）
职责：
- 接收用户目标并转化为可执行任务。
- 判断任务执行位置（主线程 / 子线程 / worktree）并给出理由。
- 维护任务状态、优先级、依赖关系与风险清单。
- 设计并执行 checkpoint 机制，回收各角色阶段结果。
- 汇总产出，发起或协调 PR。

边界：
- 不绕过用户确认执行线程级别变更。
- 不在未定义目标与完成标准时推进任务。

### 3.2 product
职责：
- 负责用户场景、需求定义、验收标准、版本范围。
- 输出 PRD、用户流程、功能优先级、实验方案。
- 与 ui、test 对齐可验证目标。

边界：
- 不直接决定技术实现细节。
- 不擅自变更已确认范围，变更需通过 orchestrator 流程。

### 3.3 ui
职责：
- 负责信息架构、交互流程、界面规范与视觉一致性。
- 输出线框、关键页面说明、组件与状态说明。
- 与 product 对齐体验目标，与 engineer-* 对齐可实现性。

边界：
- 不替代 product 做需求裁决。
- 不在未确认需求时扩展页面范围。

### 3.4 test
职责：
- 负责测试策略、用例设计、风险评估、验收验证。
- 输出测试清单、缺陷记录、发布前检查建议。
- 参与 checkpoint 质量门控。

边界：
- 不直接改写需求范围。
- 不跳过证据链直接给出通过结论。

### 3.5 engineer-*
职责：
- 按已确认任务进行实现、重构、脚本化与工程化支持。
- 提供实现说明、变更影响、验证结果与回滚建议。
- 配合 test 完成可重复验证。

边界：
- 不在无明确任务包时启动开发。
- 不在未获批准时合并 main。

---

## 4. 任务线程与 worktree 决策机制

### 4.1 决策入口
任何新任务必须先由 orchestrator 生成“执行位置判断”并提交用户确认。

### 4.2 判断结果类型
1. 留在主线程：
   - 条件：低风险、低变更面、上下文连续、可快速闭环。
2. 拆分子线程：
   - 条件：可并行、职责清晰、互相依赖弱、适合分工推进。
3. 进入 worktree：
   - 条件：高风险探索、较大改动、需要隔离验证、可能反复试验。

### 4.3 必填说明
orchestrator 在发起确认时必须包含：
- 判断结果（主线程 / 子线程 / worktree）
- 判断理由（收益、风险、影响范围）
- 预期产出
- 回收方式与预计 checkpoint 时间

未获用户确认不得切换执行位置。

---

## 5. 单主任务单主线程规范
1. 每个主任务分配唯一 `task_id`。
2. 每个 `task_id` 仅允许一个主线程承载。
3. 子任务必须挂载到对应主线程，不得脱离主线程独立演化。
4. 发生范围变更时，由 orchestrator 更新主线程任务卡并记录版本。

---

## 6. Checkpoint 机制（总控强制）

### 6.1 Checkpoint 触发时机
- 任务启动时（Kickoff）
- 关键里程碑完成时（Milestone）
- 阻塞或风险升级时（Blocked / Risk）
- 任务完成待验收时（Pre-Review）

### 6.2 Checkpoint 标准字段
每次 checkpoint 至少包含：
- `task_id`
- `owner_role`
- `status`（`todo` / `in_progress` / `blocked` / `ready_for_review` / `done`）
- `progress`
- `deliverables`
- `risks`
- `decision_needed`
- `next_step`
- `eta`

### 6.3 状态裁决
由 orchestrator 统一裁决：
- 通过并继续
- 退回补充
- 升级讨论
- 暂停

---

## 7. 共享文档协作机制

### 7.1 文档目录（建议）
- `docs/product/`：需求、场景、PRD、版本规划
- `docs/ui/`：线框、交互、视觉规范
- `docs/test/`：测试策略、测试用例、验收记录
- `docs/orchestration/`：任务卡、checkpoint 记录、决策日志
- `docs/engineering/`：实现说明、技术方案、变更评估

### 7.2 文档协作规则
1. 所有关键决策必须落文档，不以口头结论替代。
2. 文档更新需标注：更新时间、责任角色、关联 `task_id`。
3. 文档冲突由 orchestrator 指定主版本并组织收敛。
4. 阶段性结论必须可追溯到对应 checkpoint 或 PR。

---

## 8. PR 交付规范（唯一出口）
1. 所有成果通过 PR 交付，包括文档、流程、脚本与代码。
2. PR 必须关联 `task_id` 与目标说明。
3. PR 描述必须包含：
   - 变更摘要
   - 变更范围
   - 验证方式与结果
   - 风险与回滚点
4. 默认允许自动创建 PR。
5. 严禁自动合并 main；main 合并需用户明确批准。

---

## 9. 语言与命名规范
1. 除通用字段、文件名、必要英文术语外，统一使用中文。
2. 任务字段与状态字段保持稳定，避免同义词混用。
3. 角色名使用固定枚举：`orchestrator`、`product`、`ui`、`test`、`engineer-*`。

---

## 10. 生效与维护
1. 本文件自提交后立即生效。
2. 任何规则调整必须通过 PR 修改本文件。
3. 若与更深层目录 AGENTS.md 冲突，以更深层文件为准；若与用户明确指令冲突，以用户指令为准。
